"use strict";
// Exclude<U, V> -> remove from U those members assignable to V
// Extract<U, V> -> keep from U those members assignable to V
// notNullable
function handleEvent1(e) {
    console.log(e);
}
function handleEvent2(e) {
    console.log(e);
}
function sqauerFucntion(num) {
    return num * 2;
}
// sqauerFucntion(10);
// sqauerFucntion(null);
